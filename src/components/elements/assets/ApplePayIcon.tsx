import svgIcon from '../../../img/applepay.svg';
const ApplePayIcon = () => {
  return (
    <div className="apple-pay-icon flex-c-c">
      <img src={svgIcon} alt={svgIcon} />
    </div>
  );
};

export default ApplePayIcon;
