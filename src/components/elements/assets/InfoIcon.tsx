import svgIcon from '../../../img/info.svg';
const InfoIcon = () => {
  return (
    <div className="info-icon flex-c-c">
      <img src={svgIcon} alt={svgIcon} />
    </div>
  );
};

export default InfoIcon;
