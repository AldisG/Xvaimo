import svgIcon from '../../../img/clock.svg';
const ClockIcon = () => {
  return (
    <div className="clock-icon flex-c-c">
      <img src={svgIcon} alt={svgIcon} />
    </div>
  );
};

export default ClockIcon;
